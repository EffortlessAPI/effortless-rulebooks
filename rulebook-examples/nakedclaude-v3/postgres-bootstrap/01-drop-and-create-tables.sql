-- ============================================================================
-- CREATE TABLES (check-add mode) - Idempotent schema, never drops columns
-- ============================================================================
-- Generated by rulebook-to-postgres tool
-- Mode: check-add    Stage: dev
-- Total Tables: 6
-- Total Raw Fields: 44
-- ============================================================================

SET timezone = 'UTC';

-- ============================================================================
-- CREATE TABLES - Normalized data only (no calculated fields)
-- ============================================================================

-- ----------------------------------------------------------------------------
-- Customers: Table: Customers
-- ----------------------------------------------------------------------------
CREATE TABLE IF NOT EXISTS customers (
  customer_id                         TEXT                 PRIMARY KEY
);
ALTER TABLE customers ADD COLUMN IF NOT EXISTS name TEXT;                                           -- Human-readable customer name.
ALTER TABLE customers ADD COLUMN IF NOT EXISTS notes TEXT;                                          -- Free-form notes about the customer.
ALTER TABLE customers ADD COLUMN IF NOT EXISTS status TEXT;                                         -- Link to the customer's current Status in the Statuses table.
ALTER TABLE customers ADD COLUMN IF NOT EXISTS email TEXT;                                          -- Primary email address for the customer.
ALTER TABLE customers ADD COLUMN IF NOT EXISTS phone TEXT;                                          -- Primary phone number for the customer.
ALTER TABLE customers ADD COLUMN IF NOT EXISTS company_name TEXT;                                   -- Company the customer represents, if applicable.
ALTER TABLE customers ADD COLUMN IF NOT EXISTS billing_address TEXT;                                -- Default billing address for the customer.
ALTER TABLE customers ADD COLUMN IF NOT EXISTS shipping_address TEXT;                               -- Default shipping address for the customer.
ALTER TABLE customers ADD COLUMN IF NOT EXISTS created_at TIMESTAMPTZ;                              -- Timestamp when the customer record was created.
COMMENT ON TABLE customers IS 'Table: Customers';
COMMENT ON COLUMN customers.name IS 'Human-readable customer name.';
COMMENT ON COLUMN customers.notes IS 'Free-form notes about the customer.';
COMMENT ON COLUMN customers.status IS 'Link to the customer''s current Status in the Statuses table.';
COMMENT ON COLUMN customers.email IS 'Primary email address for the customer.';
COMMENT ON COLUMN customers.phone IS 'Primary phone number for the customer.';
COMMENT ON COLUMN customers.company_name IS 'Company the customer represents, if applicable.';
COMMENT ON COLUMN customers.billing_address IS 'Default billing address for the customer.';
COMMENT ON COLUMN customers.shipping_address IS 'Default shipping address for the customer.';
COMMENT ON COLUMN customers.created_at IS 'Timestamp when the customer record was created.';

-- ----------------------------------------------------------------------------
-- Statuses: Table: Statuses
-- ----------------------------------------------------------------------------
CREATE TABLE IF NOT EXISTS statuses (
  statuse_id                          TEXT                 PRIMARY KEY
);
ALTER TABLE statuses ADD COLUMN IF NOT EXISTS display_name TEXT;                                    -- Human-readable display name of the status (e.g., "On-Hold", "In-Review").
ALTER TABLE statuses ADD COLUMN IF NOT EXISTS description TEXT;                                     -- Free-form description of what this status means and when it applies.
ALTER TABLE statuses ADD COLUMN IF NOT EXISTS is_blocking BOOLEAN;                                  -- True when customers in this status should be considered stopped (blocked from progressing).
ALTER TABLE statuses ADD COLUMN IF NOT EXISTS sort_order INTEGER;                                   -- Display order for this status in lists.
COMMENT ON TABLE statuses IS 'Table: Statuses';
COMMENT ON COLUMN statuses.display_name IS 'Human-readable display name of the status (e.g., "On-Hold", "In-Review").';
COMMENT ON COLUMN statuses.description IS 'Free-form description of what this status means and when it applies.';
COMMENT ON COLUMN statuses.is_blocking IS 'True when customers in this status should be considered stopped (blocked from progressing).';
COMMENT ON COLUMN statuses.sort_order IS 'Display order for this status in lists.';

-- ----------------------------------------------------------------------------
-- Products: Table: Products
-- ----------------------------------------------------------------------------
CREATE TABLE IF NOT EXISTS products (
  product_id                          TEXT                 PRIMARY KEY
);
ALTER TABLE products ADD COLUMN IF NOT EXISTS sku TEXT;                                             -- Stock-keeping unit identifier for the product. The natural compound key.
ALTER TABLE products ADD COLUMN IF NOT EXISTS display_name TEXT;                                    -- Human-readable product name shown in catalogs and orders.
ALTER TABLE products ADD COLUMN IF NOT EXISTS description TEXT;                                     -- Long-form product description.
ALTER TABLE products ADD COLUMN IF NOT EXISTS unit_price NUMERIC;                                   -- Default list price per unit.
ALTER TABLE products ADD COLUMN IF NOT EXISTS cost NUMERIC;                                         -- Internal cost per unit.
ALTER TABLE products ADD COLUMN IF NOT EXISTS is_active BOOLEAN;                                    -- True when the product is currently sellable.
ALTER TABLE products ADD COLUMN IF NOT EXISTS stock_quantity INTEGER;                               -- Number of units currently in stock.
COMMENT ON TABLE products IS 'Table: Products';
COMMENT ON COLUMN products.sku IS 'Stock-keeping unit identifier for the product. The natural compound key.';
COMMENT ON COLUMN products.display_name IS 'Human-readable product name shown in catalogs and orders.';
COMMENT ON COLUMN products.description IS 'Long-form product description.';
COMMENT ON COLUMN products.unit_price IS 'Default list price per unit.';
COMMENT ON COLUMN products.cost IS 'Internal cost per unit.';
COMMENT ON COLUMN products.is_active IS 'True when the product is currently sellable.';
COMMENT ON COLUMN products.stock_quantity IS 'Number of units currently in stock.';

-- ----------------------------------------------------------------------------
-- Orders: Table: Orders
-- ----------------------------------------------------------------------------
CREATE TABLE IF NOT EXISTS orders (
  order_id                            TEXT                 PRIMARY KEY
);
ALTER TABLE orders ADD COLUMN IF NOT EXISTS order_number TEXT;                                      -- Sequential or human-assigned order identifier (e.g. '1001'). Combined with Customer to form the order's natural key.
ALTER TABLE orders ADD COLUMN IF NOT EXISTS customer TEXT;                                          -- Link to the Customer who placed this order.
ALTER TABLE orders ADD COLUMN IF NOT EXISTS order_date TIMESTAMPTZ;                                 -- When the order was placed.
ALTER TABLE orders ADD COLUMN IF NOT EXISTS order_status TEXT;                                      -- Lifecycle state of the order.
ALTER TABLE orders ADD COLUMN IF NOT EXISTS shipping_address TEXT;                                  -- Where this specific order ships to.
ALTER TABLE orders ADD COLUMN IF NOT EXISTS billing_address TEXT;                                   -- Billing address used for this specific order.
ALTER TABLE orders ADD COLUMN IF NOT EXISTS notes TEXT;                                             -- Free-form notes about this order.
ALTER TABLE orders ADD COLUMN IF NOT EXISTS tax_rate NUMERIC;                                       -- Sales tax rate applied to this order.
COMMENT ON TABLE orders IS 'Table: Orders';
COMMENT ON COLUMN orders.order_number IS 'Sequential or human-assigned order identifier (e.g. ''1001''). Combined with Customer to form the order''s natural key.';
COMMENT ON COLUMN orders.customer IS 'Link to the Customer who placed this order.';
COMMENT ON COLUMN orders.order_date IS 'When the order was placed.';
COMMENT ON COLUMN orders.order_status IS 'Lifecycle state of the order.';
COMMENT ON COLUMN orders.shipping_address IS 'Where this specific order ships to.';
COMMENT ON COLUMN orders.billing_address IS 'Billing address used for this specific order.';
COMMENT ON COLUMN orders.notes IS 'Free-form notes about this order.';
COMMENT ON COLUMN orders.tax_rate IS 'Sales tax rate applied to this order.';

-- ----------------------------------------------------------------------------
-- OrderLineItems: Table: OrderLineItems
-- ----------------------------------------------------------------------------
CREATE TABLE IF NOT EXISTS order_line_items (
  order_line_item_id                  TEXT                 PRIMARY KEY
);
ALTER TABLE order_line_items ADD COLUMN IF NOT EXISTS line_number TEXT;                             -- Position of this line within its parent Order (e.g. '1', '2'). Combined with Order to form the line item's natural key.
ALTER TABLE order_line_items ADD COLUMN IF NOT EXISTS "order" TEXT;                                 -- Link to the parent Order this line item belongs to.
ALTER TABLE order_line_items ADD COLUMN IF NOT EXISTS product TEXT;                                 -- Link to the Product being ordered on this line.
ALTER TABLE order_line_items ADD COLUMN IF NOT EXISTS quantity INTEGER;                             -- Number of units ordered on this line.
ALTER TABLE order_line_items ADD COLUMN IF NOT EXISTS unit_price NUMERIC;                           -- Price per unit captured at the time of ordering (may differ from current Product.UnitPrice).
ALTER TABLE order_line_items ADD COLUMN IF NOT EXISTS discount_percent NUMERIC;                     -- Per-line discount applied to this item.
ALTER TABLE order_line_items ADD COLUMN IF NOT EXISTS notes TEXT;                                   -- Free-form notes about this line item.
COMMENT ON TABLE order_line_items IS 'Table: OrderLineItems';
COMMENT ON COLUMN order_line_items.line_number IS 'Position of this line within its parent Order (e.g. ''1'', ''2''). Combined with Order to form the line item''s natural key.';
COMMENT ON COLUMN order_line_items."order" IS 'Link to the parent Order this line item belongs to.';
COMMENT ON COLUMN order_line_items.product IS 'Link to the Product being ordered on this line.';
COMMENT ON COLUMN order_line_items.quantity IS 'Number of units ordered on this line.';
COMMENT ON COLUMN order_line_items.unit_price IS 'Price per unit captured at the time of ordering (may differ from current Product.UnitPrice).';
COMMENT ON COLUMN order_line_items.discount_percent IS 'Per-line discount applied to this item.';
COMMENT ON COLUMN order_line_items.notes IS 'Free-form notes about this line item.';

-- ----------------------------------------------------------------------------
-- Payments: Table: Payments
-- ----------------------------------------------------------------------------
CREATE TABLE IF NOT EXISTS payments (
  payment_id                          TEXT                 PRIMARY KEY
);
ALTER TABLE payments ADD COLUMN IF NOT EXISTS payment_number TEXT;                                  -- Sequential payment identifier within the parent Order (e.g. '1', '2'). Combined with Order to form the payment's natural key.
ALTER TABLE payments ADD COLUMN IF NOT EXISTS "order" TEXT;                                         -- Link to the Order this payment is being applied to.
ALTER TABLE payments ADD COLUMN IF NOT EXISTS payment_date TIMESTAMPTZ;                             -- When the payment was received.
ALTER TABLE payments ADD COLUMN IF NOT EXISTS payment_method TEXT;                                  -- How the payment was made.
ALTER TABLE payments ADD COLUMN IF NOT EXISTS transaction_id TEXT;                                  -- External processor transaction identifier.
ALTER TABLE payments ADD COLUMN IF NOT EXISTS notes TEXT;                                           -- Free-form notes about this payment.
ALTER TABLE payments ADD COLUMN IF NOT EXISTS amount NUMERIC;                                       -- Amount paid.
ALTER TABLE payments ADD COLUMN IF NOT EXISTS payment_status TEXT;                                  -- Lifecycle state of the payment.
COMMENT ON TABLE payments IS 'Table: Payments';
COMMENT ON COLUMN payments.payment_number IS 'Sequential payment identifier within the parent Order (e.g. ''1'', ''2''). Combined with Order to form the payment''s natural key.';
COMMENT ON COLUMN payments."order" IS 'Link to the Order this payment is being applied to.';
COMMENT ON COLUMN payments.payment_date IS 'When the payment was received.';
COMMENT ON COLUMN payments.payment_method IS 'How the payment was made.';
COMMENT ON COLUMN payments.transaction_id IS 'External processor transaction identifier.';
COMMENT ON COLUMN payments.notes IS 'Free-form notes about this payment.';
COMMENT ON COLUMN payments.amount IS 'Amount paid.';
COMMENT ON COLUMN payments.payment_status IS 'Lifecycle state of the payment.';

-- ============================================================================
-- FK INDEXES - Performance only, always applied
-- ============================================================================

-- Customers
CREATE INDEX IF NOT EXISTS idx_customers_status ON customers (status);

-- Statuses
CREATE INDEX IF NOT EXISTS idx_statuses_display_name ON statuses (display_name);

-- Products
CREATE INDEX IF NOT EXISTS idx_products_sku ON products (sku);

-- Orders
CREATE INDEX IF NOT EXISTS idx_orders_customer ON orders (customer);

-- OrderLineItems
CREATE INDEX IF NOT EXISTS idx_order_line_items_order ON order_line_items ("order");
CREATE INDEX IF NOT EXISTS idx_order_line_items_product ON order_line_items (product);

-- Payments
CREATE INDEX IF NOT EXISTS idx_payments_order ON payments ("order");

-- 7 FK index(es) declared.
