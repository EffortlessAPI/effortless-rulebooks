# Generated effortless.env for domain: talismans-special-solutions
# DATABASE_URL is the single source of truth for this domain's database connection.
# Edit only if you need to override (e.g., remote postgres, different credentials).

DATABASE_URL=postgresql://postgres@localhost:5432/erb_talismans_special_solutions
