# Timestamped run logs are local audit artifacts — noisy to track. The admin UI
# reads latest.json (committed) for the last-known baseline; full history lives
# on disk locally and can be regenerated by re-running the harness.
run-*.json
