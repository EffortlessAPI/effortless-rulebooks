-- ============================================================================
-- CREATE TABLES (check-add mode) - Idempotent schema, never drops columns
-- ============================================================================
-- Generated by rulebook-to-postgres tool
-- Mode: check-add    Stage: dev
-- Total Tables: 13
-- Total Raw Fields: 71
-- ============================================================================

SET timezone = 'UTC';

-- ============================================================================
-- CREATE TABLES - Normalized data only (no calculated fields)
-- ============================================================================

-- ----------------------------------------------------------------------------
-- AppUsers: Table: AppUsers
-- ----------------------------------------------------------------------------
CREATE TABLE IF NOT EXISTS app_users (
  app_user_id                         TEXT                 PRIMARY KEY
);
ALTER TABLE app_users ADD COLUMN IF NOT EXISTS contact_name TEXT;
ALTER TABLE app_users ADD COLUMN IF NOT EXISTS email_address TEXT;
ALTER TABLE app_users ADD COLUMN IF NOT EXISTS phone_number TEXT;
ALTER TABLE app_users ADD COLUMN IF NOT EXISTS role TEXT;
ALTER TABLE app_users ADD COLUMN IF NOT EXISTS notes TEXT;
COMMENT ON TABLE app_users IS 'Table: AppUsers';

-- ----------------------------------------------------------------------------
-- Clients: Table: Clients
-- ----------------------------------------------------------------------------
CREATE TABLE IF NOT EXISTS clients (
  client_id                           TEXT                 PRIMARY KEY
);
ALTER TABLE clients ADD COLUMN IF NOT EXISTS name TEXT;                                             -- Human-readable customer name.
ALTER TABLE clients ADD COLUMN IF NOT EXISTS notes TEXT;                                            -- Free-form notes about the customer.
ALTER TABLE clients ADD COLUMN IF NOT EXISTS category TEXT;
ALTER TABLE clients ADD COLUMN IF NOT EXISTS status TEXT;                                           -- Link to the customer's current Status in the Statuses table.
ALTER TABLE clients ADD COLUMN IF NOT EXISTS email TEXT;                                            -- Primary email address for the customer.
ALTER TABLE clients ADD COLUMN IF NOT EXISTS phone TEXT;                                            -- Primary phone number for the customer.
ALTER TABLE clients ADD COLUMN IF NOT EXISTS company_name TEXT;                                     -- Company the customer represents, if applicable.
ALTER TABLE clients ADD COLUMN IF NOT EXISTS addresses TEXT;
ALTER TABLE clients ADD COLUMN IF NOT EXISTS created_at TIMESTAMPTZ;                                -- Timestamp when the customer record was created.
COMMENT ON TABLE clients IS 'Table: Clients';
COMMENT ON COLUMN clients.name IS 'Human-readable customer name.';
COMMENT ON COLUMN clients.notes IS 'Free-form notes about the customer.';
COMMENT ON COLUMN clients.status IS 'Link to the customer''s current Status in the Statuses table.';
COMMENT ON COLUMN clients.email IS 'Primary email address for the customer.';
COMMENT ON COLUMN clients.phone IS 'Primary phone number for the customer.';
COMMENT ON COLUMN clients.company_name IS 'Company the customer represents, if applicable.';
COMMENT ON COLUMN clients.created_at IS 'Timestamp when the customer record was created.';

-- ----------------------------------------------------------------------------
-- ClientApprovals: Table: ClientApprovals
-- ----------------------------------------------------------------------------
CREATE TABLE IF NOT EXISTS client_approvals (
  client_approval_id                  TEXT                 PRIMARY KEY
);
ALTER TABLE client_approvals ADD COLUMN IF NOT EXISTS client TEXT;
ALTER TABLE client_approvals ADD COLUMN IF NOT EXISTS approved_by TEXT;
ALTER TABLE client_approvals ADD COLUMN IF NOT EXISTS notes TEXT;
COMMENT ON TABLE client_approvals IS 'Table: ClientApprovals';

-- ----------------------------------------------------------------------------
-- ClientCategories: Table: ClientCategories
-- ----------------------------------------------------------------------------
CREATE TABLE IF NOT EXISTS client_categories (
  client_categorie_id                 TEXT                 PRIMARY KEY
);
ALTER TABLE client_categories ADD COLUMN IF NOT EXISTS name TEXT;
ALTER TABLE client_categories ADD COLUMN IF NOT EXISTS discount NUMERIC;
ALTER TABLE client_categories ADD COLUMN IF NOT EXISTS notes TEXT;
COMMENT ON TABLE client_categories IS 'Table: ClientCategories';

-- ----------------------------------------------------------------------------
-- Statuses: Table: Statuses
-- ----------------------------------------------------------------------------
CREATE TABLE IF NOT EXISTS statuses (
  statuse_id                          TEXT                 PRIMARY KEY
);
ALTER TABLE statuses ADD COLUMN IF NOT EXISTS display_name TEXT;                                    -- Human-readable display name of the status (e.g., "On-Hold", "In-Review").
ALTER TABLE statuses ADD COLUMN IF NOT EXISTS description TEXT;                                     -- Free-form description of what this status means and when it applies.
ALTER TABLE statuses ADD COLUMN IF NOT EXISTS is_blocking BOOLEAN;                                  -- True when customers in this status should be considered stopped (blocked from progressing).
ALTER TABLE statuses ADD COLUMN IF NOT EXISTS sort_order INTEGER;                                   -- Display order for this status in lists.
COMMENT ON TABLE statuses IS 'Table: Statuses';
COMMENT ON COLUMN statuses.display_name IS 'Human-readable display name of the status (e.g., "On-Hold", "In-Review").';
COMMENT ON COLUMN statuses.description IS 'Free-form description of what this status means and when it applies.';
COMMENT ON COLUMN statuses.is_blocking IS 'True when customers in this status should be considered stopped (blocked from progressing).';
COMMENT ON COLUMN statuses.sort_order IS 'Display order for this status in lists.';

-- ----------------------------------------------------------------------------
-- Products: Table: Products
-- ----------------------------------------------------------------------------
CREATE TABLE IF NOT EXISTS products (
  product_id                          TEXT                 PRIMARY KEY
);
ALTER TABLE products ADD COLUMN IF NOT EXISTS sku TEXT;                                             -- Stock-keeping unit identifier for the product. The natural compound key.
ALTER TABLE products ADD COLUMN IF NOT EXISTS display_name TEXT;                                    -- Human-readable product name shown in catalogs and orders.
ALTER TABLE products ADD COLUMN IF NOT EXISTS description TEXT;                                     -- Long-form product description.
ALTER TABLE products ADD COLUMN IF NOT EXISTS unit_price NUMERIC;                                   -- Default list price per unit.
ALTER TABLE products ADD COLUMN IF NOT EXISTS cost NUMERIC;                                         -- Internal cost per unit.
ALTER TABLE products ADD COLUMN IF NOT EXISTS is_active BOOLEAN;                                    -- True when the product is currently sellable.
ALTER TABLE products ADD COLUMN IF NOT EXISTS inventory_adjustments TEXT;
COMMENT ON TABLE products IS 'Table: Products';
COMMENT ON COLUMN products.sku IS 'Stock-keeping unit identifier for the product. The natural compound key.';
COMMENT ON COLUMN products.display_name IS 'Human-readable product name shown in catalogs and orders.';
COMMENT ON COLUMN products.description IS 'Long-form product description.';
COMMENT ON COLUMN products.unit_price IS 'Default list price per unit.';
COMMENT ON COLUMN products.cost IS 'Internal cost per unit.';
COMMENT ON COLUMN products.is_active IS 'True when the product is currently sellable.';

-- ----------------------------------------------------------------------------
-- Invoices: Table: Invoices
-- ----------------------------------------------------------------------------
CREATE TABLE IF NOT EXISTS invoices (
  invoice_id                          TEXT                 PRIMARY KEY
);
ALTER TABLE invoices ADD COLUMN IF NOT EXISTS invoice_number TEXT;                                  -- Sequential or human-assigned order identifier (e.g. '1001'). Combined with Customer to form the order's natural key.
ALTER TABLE invoices ADD COLUMN IF NOT EXISTS client TEXT;                                          -- Link to the Customer who placed this order.
ALTER TABLE invoices ADD COLUMN IF NOT EXISTS order_date TIMESTAMPTZ;                               -- When the order was placed.
ALTER TABLE invoices ADD COLUMN IF NOT EXISTS order_status TEXT;                                    -- Lifecycle state of the order.
ALTER TABLE invoices ADD COLUMN IF NOT EXISTS shipping_address TEXT;                                -- Where this specific order ships to.
ALTER TABLE invoices ADD COLUMN IF NOT EXISTS billing_address TEXT;                                 -- Billing address used for this specific order.
ALTER TABLE invoices ADD COLUMN IF NOT EXISTS notes TEXT;                                           -- Free-form notes about this order.
COMMENT ON TABLE invoices IS 'Table: Invoices';
COMMENT ON COLUMN invoices.invoice_number IS 'Sequential or human-assigned order identifier (e.g. ''1001''). Combined with Customer to form the order''s natural key.';
COMMENT ON COLUMN invoices.client IS 'Link to the Customer who placed this order.';
COMMENT ON COLUMN invoices.order_date IS 'When the order was placed.';
COMMENT ON COLUMN invoices.order_status IS 'Lifecycle state of the order.';
COMMENT ON COLUMN invoices.shipping_address IS 'Where this specific order ships to.';
COMMENT ON COLUMN invoices.billing_address IS 'Billing address used for this specific order.';
COMMENT ON COLUMN invoices.notes IS 'Free-form notes about this order.';

-- ----------------------------------------------------------------------------
-- InvoiceLineItems: Table: InvoiceLineItems
-- ----------------------------------------------------------------------------
CREATE TABLE IF NOT EXISTS invoice_line_items (
  invoice_line_item_id                TEXT                 PRIMARY KEY
);
ALTER TABLE invoice_line_items ADD COLUMN IF NOT EXISTS line_number TEXT;                           -- Position of this line within its parent Order (e.g. '1', '2'). Combined with Order to form the line item's natural key.
ALTER TABLE invoice_line_items ADD COLUMN IF NOT EXISTS invoice TEXT;                               -- Link to the parent Order this line item belongs to.
ALTER TABLE invoice_line_items ADD COLUMN IF NOT EXISTS product TEXT;                               -- Link to the Product being ordered on this line.
ALTER TABLE invoice_line_items ADD COLUMN IF NOT EXISTS quantity INTEGER;                           -- Number of units ordered on this line.
ALTER TABLE invoice_line_items ADD COLUMN IF NOT EXISTS notes TEXT;                                 -- Free-form notes about this line item.
COMMENT ON TABLE invoice_line_items IS 'Table: InvoiceLineItems';
COMMENT ON COLUMN invoice_line_items.line_number IS 'Position of this line within its parent Order (e.g. ''1'', ''2''). Combined with Order to form the line item''s natural key.';
COMMENT ON COLUMN invoice_line_items.invoice IS 'Link to the parent Order this line item belongs to.';
COMMENT ON COLUMN invoice_line_items.product IS 'Link to the Product being ordered on this line.';
COMMENT ON COLUMN invoice_line_items.quantity IS 'Number of units ordered on this line.';
COMMENT ON COLUMN invoice_line_items.notes IS 'Free-form notes about this line item.';

-- ----------------------------------------------------------------------------
-- InventoryAdjustments: Table: InventoryAdjustments
-- ----------------------------------------------------------------------------
CREATE TABLE IF NOT EXISTS inventory_adjustments (
  inventory_adjustment_id             TEXT                 PRIMARY KEY
);
ALTER TABLE inventory_adjustments ADD COLUMN IF NOT EXISTS product TEXT;
ALTER TABLE inventory_adjustments ADD COLUMN IF NOT EXISTS date TIMESTAMPTZ;
ALTER TABLE inventory_adjustments ADD COLUMN IF NOT EXISTS adjustment_type TEXT;
ALTER TABLE inventory_adjustments ADD COLUMN IF NOT EXISTS quantity INTEGER;
ALTER TABLE inventory_adjustments ADD COLUMN IF NOT EXISTS reason TEXT;
ALTER TABLE inventory_adjustments ADD COLUMN IF NOT EXISTS notes TEXT;
ALTER TABLE inventory_adjustments ADD COLUMN IF NOT EXISTS adjusted_by TEXT;
COMMENT ON TABLE inventory_adjustments IS 'Table: InventoryAdjustments';

-- ----------------------------------------------------------------------------
-- Payments: Table: Payments
-- ----------------------------------------------------------------------------
CREATE TABLE IF NOT EXISTS payments (
  payment_id                          TEXT                 PRIMARY KEY
);
ALTER TABLE payments ADD COLUMN IF NOT EXISTS payment_number TEXT;                                  -- Sequential payment identifier within the parent Order (e.g. '1', '2'). Combined with Order to form the payment's natural key.
ALTER TABLE payments ADD COLUMN IF NOT EXISTS invoice TEXT;                                         -- Link to the Order this payment is being applied to.
ALTER TABLE payments ADD COLUMN IF NOT EXISTS payment_date TIMESTAMPTZ;                             -- When the payment was received.
ALTER TABLE payments ADD COLUMN IF NOT EXISTS payment_method TEXT;                                  -- How the payment was made.
ALTER TABLE payments ADD COLUMN IF NOT EXISTS transaction_id TEXT;                                  -- External processor transaction identifier.
ALTER TABLE payments ADD COLUMN IF NOT EXISTS notes TEXT;                                           -- Free-form notes about this payment.
ALTER TABLE payments ADD COLUMN IF NOT EXISTS amount NUMERIC;                                       -- Amount paid.
ALTER TABLE payments ADD COLUMN IF NOT EXISTS payment_status TEXT;                                  -- Lifecycle state of the payment.
COMMENT ON TABLE payments IS 'Table: Payments';
COMMENT ON COLUMN payments.payment_number IS 'Sequential payment identifier within the parent Order (e.g. ''1'', ''2''). Combined with Order to form the payment''s natural key.';
COMMENT ON COLUMN payments.invoice IS 'Link to the Order this payment is being applied to.';
COMMENT ON COLUMN payments.payment_date IS 'When the payment was received.';
COMMENT ON COLUMN payments.payment_method IS 'How the payment was made.';
COMMENT ON COLUMN payments.transaction_id IS 'External processor transaction identifier.';
COMMENT ON COLUMN payments.notes IS 'Free-form notes about this payment.';
COMMENT ON COLUMN payments.amount IS 'Amount paid.';
COMMENT ON COLUMN payments.payment_status IS 'Lifecycle state of the payment.';

-- ----------------------------------------------------------------------------
-- Addresses: Table: Addresses
-- ----------------------------------------------------------------------------
CREATE TABLE IF NOT EXISTS addresses (
  addresse_id                         TEXT                 PRIMARY KEY
);
ALTER TABLE addresses ADD COLUMN IF NOT EXISTS clients TEXT;
ALTER TABLE addresses ADD COLUMN IF NOT EXISTS type_of_address TEXT;
ALTER TABLE addresses ADD COLUMN IF NOT EXISTS attention TEXT;
ALTER TABLE addresses ADD COLUMN IF NOT EXISTS address1 TEXT;
ALTER TABLE addresses ADD COLUMN IF NOT EXISTS address2 TEXT;
ALTER TABLE addresses ADD COLUMN IF NOT EXISTS city TEXT;
ALTER TABLE addresses ADD COLUMN IF NOT EXISTS state TEXT;
ALTER TABLE addresses ADD COLUMN IF NOT EXISTS zip TEXT;
COMMENT ON TABLE addresses IS 'Table: Addresses';

-- ----------------------------------------------------------------------------
-- States: Table: States
-- ----------------------------------------------------------------------------
CREATE TABLE IF NOT EXISTS states (
  state_id                            TEXT                 PRIMARY KEY
);
ALTER TABLE states ADD COLUMN IF NOT EXISTS code TEXT;
ALTER TABLE states ADD COLUMN IF NOT EXISTS text TEXT;
ALTER TABLE states ADD COLUMN IF NOT EXISTS tax_rate NUMERIC;
COMMENT ON TABLE states IS 'Table: States';

-- ----------------------------------------------------------------------------
-- TypesOfAddresses: Table: TypesOfAddresses
-- ----------------------------------------------------------------------------
CREATE TABLE IF NOT EXISTS types_of_addresses (
  types_of_addresse_id                TEXT                 PRIMARY KEY
);
ALTER TABLE types_of_addresses ADD COLUMN IF NOT EXISTS name TEXT;
ALTER TABLE types_of_addresses ADD COLUMN IF NOT EXISTS is_shipping_address BOOLEAN;
ALTER TABLE types_of_addresses ADD COLUMN IF NOT EXISTS is_billing_address BOOLEAN;
COMMENT ON TABLE types_of_addresses IS 'Table: TypesOfAddresses';

-- ============================================================================
-- FK INDEXES - Performance only, always applied
-- ============================================================================

-- Clients
CREATE INDEX IF NOT EXISTS idx_clients_category ON clients (category);
CREATE INDEX IF NOT EXISTS idx_clients_status ON clients (status);
CREATE INDEX IF NOT EXISTS idx_clients_addresses ON clients (addresses);

-- ClientApprovals
CREATE INDEX IF NOT EXISTS idx_client_approvals_client ON client_approvals (client);
CREATE INDEX IF NOT EXISTS idx_client_approvals_approved_by ON client_approvals (approved_by);

-- Statuses
CREATE INDEX IF NOT EXISTS idx_statuses_display_name ON statuses (display_name);

-- Products
CREATE INDEX IF NOT EXISTS idx_products_sku ON products (sku);
CREATE INDEX IF NOT EXISTS idx_products_inventory_adjustments ON products (inventory_adjustments);

-- Invoices
CREATE INDEX IF NOT EXISTS idx_invoices_client ON invoices (client);

-- InvoiceLineItems
CREATE INDEX IF NOT EXISTS idx_invoice_line_items_invoice ON invoice_line_items (invoice);
CREATE INDEX IF NOT EXISTS idx_invoice_line_items_product ON invoice_line_items (product);

-- InventoryAdjustments
CREATE INDEX IF NOT EXISTS idx_inventory_adjustments_product ON inventory_adjustments (product);

-- Payments
CREATE INDEX IF NOT EXISTS idx_payments_invoice ON payments (invoice);

-- Addresses
CREATE INDEX IF NOT EXISTS idx_addresses_clients ON addresses (clients);
CREATE INDEX IF NOT EXISTS idx_addresses_type_of_address ON addresses (type_of_address);
CREATE INDEX IF NOT EXISTS idx_addresses_state ON addresses (state);

-- 16 FK index(es) declared.
