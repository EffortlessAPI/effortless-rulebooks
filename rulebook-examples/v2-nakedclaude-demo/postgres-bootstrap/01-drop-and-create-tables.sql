-- ============================================================================
-- CREATE TABLES (check-add mode) - Idempotent schema, never drops columns
-- ============================================================================
-- Generated by rulebook-to-postgres tool
-- Mode: check-add    Stage: dev
-- Total Tables: 3
-- Total Raw Fields: 13
-- ============================================================================

SET timezone = 'UTC';

-- ============================================================================
-- CREATE TABLES - Normalized data only (no calculated fields)
-- ============================================================================

-- ----------------------------------------------------------------------------
-- Customers: Table: Customers
-- ----------------------------------------------------------------------------
CREATE TABLE IF NOT EXISTS customers (
  customer_id                         TEXT                 PRIMARY KEY
);
ALTER TABLE customers ADD COLUMN IF NOT EXISTS first_name TEXT;
ALTER TABLE customers ADD COLUMN IF NOT EXISTS last_name TEXT;
ALTER TABLE customers ADD COLUMN IF NOT EXISTS notes TEXT;
ALTER TABLE customers ADD COLUMN IF NOT EXISTS status TEXT;
ALTER TABLE customers ADD COLUMN IF NOT EXISTS owner_email TEXT;                                    -- Email address of the user who owns this customer record. Used by Row-Level Security to scope visibility to the JWT email of the requester.
COMMENT ON TABLE customers IS 'Table: Customers';
COMMENT ON COLUMN customers.owner_email IS 'Email address of the user who owns this customer record. Used by Row-Level Security to scope visibility to the JWT email of the requester.';

-- ----------------------------------------------------------------------------
-- Statuses: Table: Statuses
-- ----------------------------------------------------------------------------
CREATE TABLE IF NOT EXISTS statuses (
  statuse_id                          TEXT                 PRIMARY KEY
);
ALTER TABLE statuses ADD COLUMN IF NOT EXISTS display_name TEXT;
ALTER TABLE statuses ADD COLUMN IF NOT EXISTS description TEXT;
ALTER TABLE statuses ADD COLUMN IF NOT EXISTS is_blocking BOOLEAN;
ALTER TABLE statuses ADD COLUMN IF NOT EXISTS customers TEXT;
ALTER TABLE statuses ADD COLUMN IF NOT EXISTS owner_email TEXT;                                     -- Email address of the user who owns this status. Used by Row-Level Security to scope visibility.
COMMENT ON TABLE statuses IS 'Table: Statuses';
COMMENT ON COLUMN statuses.owner_email IS 'Email address of the user who owns this status. Used by Row-Level Security to scope visibility.';

-- ----------------------------------------------------------------------------
-- AppUsers: Table: AppUsers
-- ----------------------------------------------------------------------------
CREATE TABLE IF NOT EXISTS app_users (
  app_user_id                         TEXT                 PRIMARY KEY
);
ALTER TABLE app_users ADD COLUMN IF NOT EXISTS email_address TEXT;                                  -- Primary identity — matches the JWT email of the signed-in user.
ALTER TABLE app_users ADD COLUMN IF NOT EXISTS role TEXT;                                           -- admin = full access; customer = sees only owned customers (RLS DAG).
COMMENT ON TABLE app_users IS 'Table: AppUsers';
COMMENT ON COLUMN app_users.email_address IS 'Primary identity — matches the JWT email of the signed-in user.';
COMMENT ON COLUMN app_users.role IS 'admin = full access; customer = sees only owned customers (RLS DAG).';

-- ============================================================================
-- FK INDEXES - Performance only, always applied
-- ============================================================================

-- Customers
CREATE INDEX IF NOT EXISTS idx_customers_status ON customers (status);

-- Statuses
CREATE INDEX IF NOT EXISTS idx_statuses_display_name ON statuses (display_name);
CREATE INDEX IF NOT EXISTS idx_statuses_customers ON statuses (customers);

-- 3 FK index(es) declared.
