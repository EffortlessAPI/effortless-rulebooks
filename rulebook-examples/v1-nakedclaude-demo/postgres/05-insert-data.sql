-- ============================================================================
-- INSERT DATA - Load data from rulebook
-- ============================================================================
-- Generated by rulebook-to-postgres tool
-- Mode: check-add    Stage: dev
-- ============================================================================

-- ----------------------------------------------------------------------------
-- Customers: Table: Customers
-- ----------------------------------------------------------------------------
INSERT INTO customers (customer_id, name, current_color, notes)
VALUES ('bob', 'Bob', 'Green', 'A customer currently in launch.') ON CONFLICT (customer_id) DO NOTHING;

INSERT INTO customers (customer_id, name, current_color, notes)
VALUES ('alice-johnson', 'Alice Johnson', 'Red', 'Initial mock entry for Alice.') ON CONFLICT (customer_id) DO NOTHING;

INSERT INTO customers (customer_id, name, current_color, notes)
VALUES ('brian-lee', 'Brian Lee', 'Blue', 'Initial mock entry for Brian.') ON CONFLICT (customer_id) DO NOTHING;

INSERT INTO customers (customer_id, name, current_color, notes)
VALUES ('carla-smith', 'Carla Smith', 'Yellow', 'Initial mock entry for Carla.') ON CONFLICT (customer_id) DO NOTHING;

INSERT INTO customers (customer_id, name, current_color, notes)
VALUES ('caroline', 'Caroline', 'Red', '') ON CONFLICT (customer_id) DO NOTHING;

