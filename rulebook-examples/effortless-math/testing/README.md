# Testing

`expected-state.json` is the frozen migration answer key. The first integration PR is successful only when a blank rulebook build reproduces the semantic counts and statuses across every enabled substrate.

The counts are not a substitute for row-level conformance. They are the first smoke test; Claude should also compare stable IDs, theorem statuses, provider edges, contradiction derivation, and critical invariant failures.
