-- ============================================================================
-- CREATE TABLES (check-add mode) - Idempotent schema, never drops columns
-- ============================================================================
-- Generated by rulebook-to-postgres tool
-- Mode: check-add    Stage: dev
-- Total Tables: 3
-- Total Raw Fields: 10
-- ============================================================================

SET timezone = 'UTC';

-- ============================================================================
-- CREATE TABLES - Normalized data only (no calculated fields)
-- ============================================================================

-- ----------------------------------------------------------------------------
-- GateTypes: Table: GateTypes
-- ----------------------------------------------------------------------------
CREATE TABLE IF NOT EXISTS gate_types (
  gate_type_id                        TEXT                 PRIMARY KEY          -- gate_type_id
);
ALTER TABLE gate_types ADD COLUMN IF NOT EXISTS symbol TEXT;                                        -- Human-readable gate symbol
ALTER TABLE gate_types ADD COLUMN IF NOT EXISTS description TEXT;                                   -- What the gate does
COMMENT ON COLUMN gate_types.gate_type_id IS 'gate_type_id';
COMMENT ON COLUMN gate_types.symbol IS 'Human-readable gate symbol';
COMMENT ON COLUMN gate_types.description IS 'What the gate does';

-- ----------------------------------------------------------------------------
-- GateTruthRows: Table: GateTruthRows
-- ----------------------------------------------------------------------------
CREATE TABLE IF NOT EXISTS gate_truth_rows (
  truth_row_id                        TEXT                 PRIMARY KEY          -- truth_row_id
);
ALTER TABLE gate_truth_rows ADD COLUMN IF NOT EXISTS gate_type_id TEXT;                             -- Which gate this truth row belongs to
ALTER TABLE gate_truth_rows ADD COLUMN IF NOT EXISTS in0 INTEGER;                                   -- Left input bit
ALTER TABLE gate_truth_rows ADD COLUMN IF NOT EXISTS in1 INTEGER;                                   -- Right input bit
ALTER TABLE gate_truth_rows ADD COLUMN IF NOT EXISTS out_bit INTEGER;                               -- The gate's output bit for this input combination
COMMENT ON COLUMN gate_truth_rows.truth_row_id IS 'truth_row_id';
COMMENT ON COLUMN gate_truth_rows.gate_type_id IS 'Which gate this truth row belongs to';
COMMENT ON COLUMN gate_truth_rows.in0 IS 'Left input bit';
COMMENT ON COLUMN gate_truth_rows.in1 IS 'Right input bit';
COMMENT ON COLUMN gate_truth_rows.out_bit IS 'The gate''s output bit for this input combination';

-- ----------------------------------------------------------------------------
-- Computations: Table: Computations
-- ----------------------------------------------------------------------------
CREATE TABLE IF NOT EXISTS computations (
  computation_id                      TEXT                 PRIMARY KEY          -- computation_id
);
ALTER TABLE computations ADD COLUMN IF NOT EXISTS gate_type_id TEXT;                                -- Which gate this computation exercises (XOR for sum, AND for carry)
ALTER TABLE computations ADD COLUMN IF NOT EXISTS a INTEGER;                                        -- Input bit a (the ONLY thing the app supplies besides which gate)
ALTER TABLE computations ADD COLUMN IF NOT EXISTS b INTEGER;                                        -- Input bit b
COMMENT ON COLUMN computations.computation_id IS 'computation_id';
COMMENT ON COLUMN computations.gate_type_id IS 'Which gate this computation exercises (XOR for sum, AND for carry)';
COMMENT ON COLUMN computations.a IS 'Input bit a (the ONLY thing the app supplies besides which gate)';
COMMENT ON COLUMN computations.b IS 'Input bit b';

-- ============================================================================
-- FK INDEXES - Performance only, always applied
-- ============================================================================

-- GateTruthRows
CREATE INDEX IF NOT EXISTS idx_gate_truth_rows_gate_type_id ON gate_truth_rows (gate_type_id);

-- Computations
CREATE INDEX IF NOT EXISTS idx_computations_gate_type_id ON computations (gate_type_id);

-- 2 FK index(es) declared.
