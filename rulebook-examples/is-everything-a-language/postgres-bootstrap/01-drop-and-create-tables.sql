-- ============================================================================
-- CREATE TABLES (check-add mode) - Idempotent schema, never drops columns
-- ============================================================================
-- Generated by rulebook-to-postgres tool
-- Mode: check-add    Stage: dev
-- Total Tables: 2
-- Total Raw Fields: 29
-- ============================================================================

SET timezone = 'UTC';

-- ============================================================================
-- CREATE TABLES - Normalized data only (no calculated fields)
-- ============================================================================

-- ----------------------------------------------------------------------------
-- LanguageCandidates: Table: LanguageCandidates
-- ----------------------------------------------------------------------------
CREATE TABLE IF NOT EXISTS language_candidates (
  language_candidate_id               TEXT                 PRIMARY KEY
);
ALTER TABLE language_candidates ADD COLUMN IF NOT EXISTS name TEXT;                                 -- Name of the language candidate being classified.
ALTER TABLE language_candidates ADD COLUMN IF NOT EXISTS is_language BOOLEAN;                       -- The test answer that is intended to match the Family Feud answer to be "correct".
ALTER TABLE language_candidates ADD COLUMN IF NOT EXISTS has_syntax BOOLEAN;                        -- Does this language candidate have syntax and/or grammar?
ALTER TABLE language_candidates ADD COLUMN IF NOT EXISTS can_be_held BOOLEAN;                       -- Is this candidate physical/material.  I.e. could it at least theoretically "be held"?
ALTER TABLE language_candidates ADD COLUMN IF NOT EXISTS category TEXT;                             -- The general high level category of the candidate.
ALTER TABLE language_candidates ADD COLUMN IF NOT EXISTS has_identity BOOLEAN;                      -- Could this thing be assigned a guid, unique in the universe that would identify it globally?  Like a drivers license or social security number for a person.
ALTER TABLE language_candidates ADD COLUMN IF NOT EXISTS is_parsed BOOLEAN;                         -- Is the knowledge/information encoded in a form that requires parsing before meaning can be extracted?
ALTER TABLE language_candidates ADD COLUMN IF NOT EXISTS resolves_to_an_ast BOOLEAN;                -- Is the knowledge/information encoded in a form that requires parsing before meaning can be extracted?
ALTER TABLE language_candidates ADD COLUMN IF NOT EXISTS has_linear_decoding_pressure BOOLEAN;
ALTER TABLE language_candidates ADD COLUMN IF NOT EXISTS is_stable_ontology_reference BOOLEAN;
ALTER TABLE language_candidates ADD COLUMN IF NOT EXISTS is_live_ontology_editor BOOLEAN;
ALTER TABLE language_candidates ADD COLUMN IF NOT EXISTS is_open_world BOOLEAN;
ALTER TABLE language_candidates ADD COLUMN IF NOT EXISTS is_closed_world BOOLEAN;
ALTER TABLE language_candidates ADD COLUMN IF NOT EXISTS distance_from_concept INTEGER;
ALTER TABLE language_candidates ADD COLUMN IF NOT EXISTS dimensionality_while_editing TEXT;
ALTER TABLE language_candidates ADD COLUMN IF NOT EXISTS model_object_facility_layer TEXT;
ALTER TABLE language_candidates ADD COLUMN IF NOT EXISTS sort_order INTEGER;
COMMENT ON TABLE language_candidates IS 'Table: LanguageCandidates';
COMMENT ON COLUMN language_candidates.name IS 'Name of the language candidate being classified.';
COMMENT ON COLUMN language_candidates.is_language IS 'The test answer that is intended to match the Family Feud answer to be "correct".';
COMMENT ON COLUMN language_candidates.has_syntax IS 'Does this language candidate have syntax and/or grammar?';
COMMENT ON COLUMN language_candidates.can_be_held IS 'Is this candidate physical/material.  I.e. could it at least theoretically "be held"?';
COMMENT ON COLUMN language_candidates.category IS 'The general high level category of the candidate.';
COMMENT ON COLUMN language_candidates.has_identity IS 'Could this thing be assigned a guid, unique in the universe that would identify it globally?  Like a drivers license or social security number for a person.';
COMMENT ON COLUMN language_candidates.is_parsed IS 'Is the knowledge/information encoded in a form that requires parsing before meaning can be extracted?';
COMMENT ON COLUMN language_candidates.resolves_to_an_ast IS 'Is the knowledge/information encoded in a form that requires parsing before meaning can be extracted?';

-- ----------------------------------------------------------------------------
-- IsEverythingALanguage: Table: IsEverythingALanguage
-- ----------------------------------------------------------------------------
CREATE TABLE IF NOT EXISTS is_everything_a_language (
  is_everything_a_language_id         TEXT                 PRIMARY KEY
);
ALTER TABLE is_everything_a_language ADD COLUMN IF NOT EXISTS name TEXT;
ALTER TABLE is_everything_a_language ADD COLUMN IF NOT EXISTS argument_name TEXT;
ALTER TABLE is_everything_a_language ADD COLUMN IF NOT EXISTS argument_category TEXT;
ALTER TABLE is_everything_a_language ADD COLUMN IF NOT EXISTS step_type TEXT;
ALTER TABLE is_everything_a_language ADD COLUMN IF NOT EXISTS statement TEXT;
ALTER TABLE is_everything_a_language ADD COLUMN IF NOT EXISTS formalization TEXT;
ALTER TABLE is_everything_a_language ADD COLUMN IF NOT EXISTS related_candidate_name TEXT;
ALTER TABLE is_everything_a_language ADD COLUMN IF NOT EXISTS related_candidate_id TEXT;
ALTER TABLE is_everything_a_language ADD COLUMN IF NOT EXISTS evidence_from_rulebook TEXT;
ALTER TABLE is_everything_a_language ADD COLUMN IF NOT EXISTS notes TEXT;
COMMENT ON TABLE is_everything_a_language IS 'Table: IsEverythingALanguage';

-- ============================================================================
-- FK INDEXES - Performance only, always applied
-- ============================================================================

-- (no FK fields detected in rulebook)
