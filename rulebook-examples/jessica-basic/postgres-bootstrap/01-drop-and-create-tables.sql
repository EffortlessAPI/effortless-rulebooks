-- ============================================================================
-- CREATE TABLES (check-add mode) - Idempotent schema, never drops columns
-- ============================================================================
-- Generated by rulebook-to-postgres tool
-- Mode: check-add    Stage: dev
-- Total Tables: 9
-- Total Raw Fields: 36
-- ============================================================================

SET timezone = 'UTC';

-- ============================================================================
-- CREATE TABLES - Normalized data only (no calculated fields)
-- ============================================================================

-- ----------------------------------------------------------------------------
-- Workflows: Table: Workflows
-- ----------------------------------------------------------------------------
CREATE TABLE IF NOT EXISTS workflows (
  workflow_id                         TEXT                 PRIMARY KEY
);
ALTER TABLE workflows ADD COLUMN IF NOT EXISTS display_name TEXT;
ALTER TABLE workflows ADD COLUMN IF NOT EXISTS title TEXT;                                          -- Human-readable title of the workflow. Maps to dct:title from Dublin Core. Example: 'Production Deployment Workflow', 'Employee Onboarding'.
ALTER TABLE workflows ADD COLUMN IF NOT EXISTS description TEXT;                                    -- Detailed description of the workflow's purpose and scope. Maps to dct:description from Dublin Core. Should explain what business goal the workflow achieves.
ALTER TABLE workflows ADD COLUMN IF NOT EXISTS identifier TEXT;                                     -- External system identifier for cross-referencing. Maps to dct:identifier from Dublin Core. This is the join key back to document management systems, ticket systems, or other operational systems.
ALTER TABLE workflows ADD COLUMN IF NOT EXISTS modified TIMESTAMPTZ;                                -- Last modification timestamp. Maps to dct:modified from Dublin Core. Critical for answering CQ5: 'Which workflows haven't been reviewed or updated in twelve months?'
COMMENT ON TABLE workflows IS 'Table: Workflows';
COMMENT ON COLUMN workflows.title IS 'Human-readable title of the workflow. Maps to dct:title from Dublin Core. Example: ''Production Deployment Workflow'', ''Employee Onboarding''.';
COMMENT ON COLUMN workflows.description IS 'Detailed description of the workflow''s purpose and scope. Maps to dct:description from Dublin Core. Should explain what business goal the workflow achieves.';
COMMENT ON COLUMN workflows.identifier IS 'External system identifier for cross-referencing. Maps to dct:identifier from Dublin Core. This is the join key back to document management systems, ticket systems, or other operational systems.';
COMMENT ON COLUMN workflows.modified IS 'Last modification timestamp. Maps to dct:modified from Dublin Core. Critical for answering CQ5: ''Which workflows haven''t been reviewed or updated in twelve months?''';

-- ----------------------------------------------------------------------------
-- WorkflowSteps: Table: WorkflowSteps
-- ----------------------------------------------------------------------------
CREATE TABLE IF NOT EXISTS workflow_steps (
  workflow_step_id                    TEXT                 PRIMARY KEY
);
ALTER TABLE workflow_steps ADD COLUMN IF NOT EXISTS display_name TEXT;
ALTER TABLE workflow_steps ADD COLUMN IF NOT EXISTS workflow TEXT;                                  -- Foreign key to the parent workflow. Represents the inverse of ntwf:hasStep, enabling navigation from step to its containing workflow (ntwf:isStepOf).
ALTER TABLE workflow_steps ADD COLUMN IF NOT EXISTS sequence_position INTEGER;                      -- Integer ordinal position of the step within its workflow. Maps to ntwf:sequencePosition, declared as owl:FunctionalProperty (each step has exactly one position). Enables positional queries.
ALTER TABLE workflow_steps ADD COLUMN IF NOT EXISTS assigned_role TEXT;                             -- Foreign key to the Role responsible for executing this step. Maps to ntwf:assignedRole. Critical for implementing Heuristic 2 (role-agent separation): steps point to roles, not directly to agents.
ALTER TABLE workflow_steps ADD COLUMN IF NOT EXISTS requires_human_approval BOOLEAN;                -- Boolean flag indicating whether a human agent must fill the assigned role. Maps to ntwf:requiresHumanApproval. Enables answering CQ3: 'Which steps require human decisions vs. AI execution?'
ALTER TABLE workflow_steps ADD COLUMN IF NOT EXISTS approval_gate TEXT;                             -- Foreign key to ApprovalGate if this step is a decision checkpoint. When populated, indicates this step blocks workflow execution until explicit authorization is given.
ALTER TABLE workflow_steps ADD COLUMN IF NOT EXISTS preceded_by_steps TEXT;                         -- Reference to steps that must complete before this step can execute. Part of the ntwf:precedesStep transitive ordering relationship.
COMMENT ON TABLE workflow_steps IS 'Table: WorkflowSteps';
COMMENT ON COLUMN workflow_steps.workflow IS 'Foreign key to the parent workflow. Represents the inverse of ntwf:hasStep, enabling navigation from step to its containing workflow (ntwf:isStepOf).';
COMMENT ON COLUMN workflow_steps.sequence_position IS 'Integer ordinal position of the step within its workflow. Maps to ntwf:sequencePosition, declared as owl:FunctionalProperty (each step has exactly one position). Enables positional queries.';
COMMENT ON COLUMN workflow_steps.assigned_role IS 'Foreign key to the Role responsible for executing this step. Maps to ntwf:assignedRole. Critical for implementing Heuristic 2 (role-agent separation): steps point to roles, not directly to agents.';
COMMENT ON COLUMN workflow_steps.requires_human_approval IS 'Boolean flag indicating whether a human agent must fill the assigned role. Maps to ntwf:requiresHumanApproval. Enables answering CQ3: ''Which steps require human decisions vs. AI execution?''';
COMMENT ON COLUMN workflow_steps.approval_gate IS 'Foreign key to ApprovalGate if this step is a decision checkpoint. When populated, indicates this step blocks workflow execution until explicit authorization is given.';
COMMENT ON COLUMN workflow_steps.preceded_by_steps IS 'Reference to steps that must complete before this step can execute. Part of the ntwf:precedesStep transitive ordering relationship.';

-- ----------------------------------------------------------------------------
-- ApprovalGates: Table: ApprovalGates
-- ----------------------------------------------------------------------------
CREATE TABLE IF NOT EXISTS approval_gates (
  approval_gate_id                    TEXT                 PRIMARY KEY
);
ALTER TABLE approval_gates ADD COLUMN IF NOT EXISTS display_name TEXT;
ALTER TABLE approval_gates ADD COLUMN IF NOT EXISTS escalation_threshold_hours INTEGER;             -- Integer number of hours that may elapse on a pending gate before the ntwf:delegatesTo chain activates. Maps to ntwf:escalationThresholdHours. Domain applies only to ApprovalGate individuals.
COMMENT ON TABLE approval_gates IS 'Table: ApprovalGates';
COMMENT ON COLUMN approval_gates.escalation_threshold_hours IS 'Integer number of hours that may elapse on a pending gate before the ntwf:delegatesTo chain activates. Maps to ntwf:escalationThresholdHours. Domain applies only to ApprovalGate individuals.';

-- ----------------------------------------------------------------------------
-- PrecedesSteps: Table: PrecedesSteps
-- ----------------------------------------------------------------------------
CREATE TABLE IF NOT EXISTS precedes_steps (
  precedes_step_id                    TEXT                 PRIMARY KEY
);
ALTER TABLE precedes_steps ADD COLUMN IF NOT EXISTS name TEXT;                                      -- Ordinal sequence number for the relationship. Used for sorting and display.
ALTER TABLE precedes_steps ADD COLUMN IF NOT EXISTS workflow_step TEXT;                             -- Foreign key to the step that comes BEFORE. The source of the 'precedes' relationship.
ALTER TABLE precedes_steps ADD COLUMN IF NOT EXISTS step_number INTEGER;
COMMENT ON TABLE precedes_steps IS 'Table: PrecedesSteps';
COMMENT ON COLUMN precedes_steps.name IS 'Ordinal sequence number for the relationship. Used for sorting and display.';
COMMENT ON COLUMN precedes_steps.workflow_step IS 'Foreign key to the step that comes BEFORE. The source of the ''precedes'' relationship.';

-- ----------------------------------------------------------------------------
-- Roles: Table: Roles
-- ----------------------------------------------------------------------------
CREATE TABLE IF NOT EXISTS roles (
  role_id                             TEXT                 PRIMARY KEY
);
ALTER TABLE roles ADD COLUMN IF NOT EXISTS display_name TEXT;
ALTER TABLE roles ADD COLUMN IF NOT EXISTS label TEXT;                                              -- Human-readable display name. Maps to rdfs:label. Per Heuristic 6: if you cannot write a clear label, you do not yet understand the concept well enough to model it.
ALTER TABLE roles ADD COLUMN IF NOT EXISTS comment TEXT;                                            -- Detailed description of the role's responsibilities and scope. Maps to rdfs:comment. Should define what the role covers, what it excludes, and how it differs from adjacent roles.
ALTER TABLE roles ADD COLUMN IF NOT EXISTS filled_by_human_agent TEXT;
ALTER TABLE roles ADD COLUMN IF NOT EXISTS filled_by_ai_agent TEXT;
ALTER TABLE roles ADD COLUMN IF NOT EXISTS filled_by_automated_pipeline TEXT;
ALTER TABLE roles ADD COLUMN IF NOT EXISTS owned_by TEXT;                                           -- Foreign key to the Department that owns this role. Maps to ntwf:ownedBy (owl:FunctionalProperty). Enables answering CQ7: 'Which workflows involve both Engineering and Legal?'
ALTER TABLE roles ADD COLUMN IF NOT EXISTS delegates_to TEXT;                                       -- Foreign key to the next Role in the escalation chain. Maps to ntwf:delegatesTo. Enables answering CQ6: 'What happens when the VP of Engineering is unavailable?'
ALTER TABLE roles ADD COLUMN IF NOT EXISTS from_delegates_to TEXT;
COMMENT ON TABLE roles IS 'Table: Roles';
COMMENT ON COLUMN roles.label IS 'Human-readable display name. Maps to rdfs:label. Per Heuristic 6: if you cannot write a clear label, you do not yet understand the concept well enough to model it.';
COMMENT ON COLUMN roles.comment IS 'Detailed description of the role''s responsibilities and scope. Maps to rdfs:comment. Should define what the role covers, what it excludes, and how it differs from adjacent roles.';
COMMENT ON COLUMN roles.owned_by IS 'Foreign key to the Department that owns this role. Maps to ntwf:ownedBy (owl:FunctionalProperty). Enables answering CQ7: ''Which workflows involve both Engineering and Legal?''';
COMMENT ON COLUMN roles.delegates_to IS 'Foreign key to the next Role in the escalation chain. Maps to ntwf:delegatesTo. Enables answering CQ6: ''What happens when the VP of Engineering is unavailable?''';

-- ----------------------------------------------------------------------------
-- Departments: Table: Departments
-- ----------------------------------------------------------------------------
CREATE TABLE IF NOT EXISTS departments (
  department_id                       TEXT                 PRIMARY KEY
);
ALTER TABLE departments ADD COLUMN IF NOT EXISTS title TEXT;                                        -- Human-readable display name of the department. Should match organizational terminology for stakeholder communication.
ALTER TABLE departments ADD COLUMN IF NOT EXISTS display_name TEXT;                                 -- Machine-friendly name for programmatic reference.
ALTER TABLE departments ADD COLUMN IF NOT EXISTS roles TEXT;                                        -- Back-reference to roles owned by this department. Inverse of Roles.OwnedBy.
COMMENT ON TABLE departments IS 'Table: Departments';
COMMENT ON COLUMN departments.title IS 'Human-readable display name of the department. Should match organizational terminology for stakeholder communication.';
COMMENT ON COLUMN departments.display_name IS 'Machine-friendly name for programmatic reference.';
COMMENT ON COLUMN departments.roles IS 'Back-reference to roles owned by this department. Inverse of Roles.OwnedBy.';

-- ----------------------------------------------------------------------------
-- HumanAgents: Table: HumanAgents
-- ----------------------------------------------------------------------------
CREATE TABLE IF NOT EXISTS human_agents (
  human_agent_id                      TEXT                 PRIMARY KEY
);
ALTER TABLE human_agents ADD COLUMN IF NOT EXISTS name TEXT;                                        -- Full name of the person. Maps to foaf:name. Note: FOAF's name property is appropriate for persons, not for software systems (which use schema:name).
ALTER TABLE human_agents ADD COLUMN IF NOT EXISTS display_name TEXT;
ALTER TABLE human_agents ADD COLUMN IF NOT EXISTS mbox TEXT;                                        -- Email address of the person. Maps to foaf:mbox. Used for notifications and organizational directory integration.
ALTER TABLE human_agents ADD COLUMN IF NOT EXISTS roles TEXT;                                       -- Back-reference to roles currently filled by this agent. Inverse of Roles.FilledBy_HumanAgent.
COMMENT ON TABLE human_agents IS 'Table: HumanAgents';
COMMENT ON COLUMN human_agents.name IS 'Full name of the person. Maps to foaf:name. Note: FOAF''s name property is appropriate for persons, not for software systems (which use schema:name).';
COMMENT ON COLUMN human_agents.mbox IS 'Email address of the person. Maps to foaf:mbox. Used for notifications and organizational directory integration.';
COMMENT ON COLUMN human_agents.roles IS 'Back-reference to roles currently filled by this agent. Inverse of Roles.FilledBy_HumanAgent.';

-- ----------------------------------------------------------------------------
-- AIAgents: Table: AIAgents
-- ----------------------------------------------------------------------------
CREATE TABLE IF NOT EXISTS ai_agents (
  ai_agent_id                         TEXT                 PRIMARY KEY
);
ALTER TABLE ai_agents ADD COLUMN IF NOT EXISTS name TEXT;                                           -- Display name of the AI agent. Maps to schema:name (not foaf:name, which is for persons).
ALTER TABLE ai_agents ADD COLUMN IF NOT EXISTS title TEXT;
ALTER TABLE ai_agents ADD COLUMN IF NOT EXISTS display_name TEXT;
ALTER TABLE ai_agents ADD COLUMN IF NOT EXISTS model_version TEXT;                                  -- Version string of the AI model. Maps to ntwf:modelVersion. Makes AI-produced artifacts auditable at the version level. The domain declaration means this property applies only to AIAgent individuals.
ALTER TABLE ai_agents ADD COLUMN IF NOT EXISTS roles TEXT;                                          -- Back-reference to roles currently filled by this AI agent. Inverse of Roles.FilledBy_AIAgent.
COMMENT ON TABLE ai_agents IS 'Table: AIAgents';
COMMENT ON COLUMN ai_agents.name IS 'Display name of the AI agent. Maps to schema:name (not foaf:name, which is for persons).';
COMMENT ON COLUMN ai_agents.model_version IS 'Version string of the AI model. Maps to ntwf:modelVersion. Makes AI-produced artifacts auditable at the version level. The domain declaration means this property applies only to AIAgent individuals.';
COMMENT ON COLUMN ai_agents.roles IS 'Back-reference to roles currently filled by this AI agent. Inverse of Roles.FilledBy_AIAgent.';

-- ----------------------------------------------------------------------------
-- AutomatedPipelines: Table: AutomatedPipelines
-- ----------------------------------------------------------------------------
CREATE TABLE IF NOT EXISTS automated_pipelines (
  automated_pipeline_id               TEXT                 PRIMARY KEY
);
ALTER TABLE automated_pipelines ADD COLUMN IF NOT EXISTS name TEXT;                                 -- Display name of the pipeline. Maps to schema:name (appropriate for software systems, unlike foaf:name which is for persons).
ALTER TABLE automated_pipelines ADD COLUMN IF NOT EXISTS description TEXT;
ALTER TABLE automated_pipelines ADD COLUMN IF NOT EXISTS display_name TEXT;
ALTER TABLE automated_pipelines ADD COLUMN IF NOT EXISTS roles TEXT;                                -- Back-reference to roles currently filled by this pipeline. Inverse of Roles.FilledBy_AutomatedPipeline.
COMMENT ON TABLE automated_pipelines IS 'Table: AutomatedPipelines';
COMMENT ON COLUMN automated_pipelines.name IS 'Display name of the pipeline. Maps to schema:name (appropriate for software systems, unlike foaf:name which is for persons).';
COMMENT ON COLUMN automated_pipelines.roles IS 'Back-reference to roles currently filled by this pipeline. Inverse of Roles.FilledBy_AutomatedPipeline.';

-- ============================================================================
-- FK INDEXES - Performance only, always applied
-- ============================================================================

-- WorkflowSteps
CREATE INDEX IF NOT EXISTS idx_workflow_steps_workflow ON workflow_steps (workflow);
CREATE INDEX IF NOT EXISTS idx_workflow_steps_assigned_role ON workflow_steps (assigned_role);
CREATE INDEX IF NOT EXISTS idx_workflow_steps_approval_gate ON workflow_steps (approval_gate);
CREATE INDEX IF NOT EXISTS idx_workflow_steps_preceded_by_steps ON workflow_steps (preceded_by_steps);

-- PrecedesSteps
CREATE INDEX IF NOT EXISTS idx_precedes_steps_workflow_step ON precedes_steps (workflow_step);

-- Roles
CREATE INDEX IF NOT EXISTS idx_roles_display_name ON roles (display_name);
CREATE INDEX IF NOT EXISTS idx_roles_filled_by_human_agent ON roles (filled_by_human_agent);
CREATE INDEX IF NOT EXISTS idx_roles_filled_by_ai_agent ON roles (filled_by_ai_agent);
CREATE INDEX IF NOT EXISTS idx_roles_filled_by_automated_pipeline ON roles (filled_by_automated_pipeline);
CREATE INDEX IF NOT EXISTS idx_roles_owned_by ON roles (owned_by);
CREATE INDEX IF NOT EXISTS idx_roles_delegates_to ON roles (delegates_to);
CREATE INDEX IF NOT EXISTS idx_roles_from_delegates_to ON roles (from_delegates_to);

-- Departments
CREATE INDEX IF NOT EXISTS idx_departments_roles ON departments (roles);

-- HumanAgents
CREATE INDEX IF NOT EXISTS idx_human_agents_roles ON human_agents (roles);

-- AIAgents
CREATE INDEX IF NOT EXISTS idx_ai_agents_display_name ON ai_agents (display_name);
CREATE INDEX IF NOT EXISTS idx_ai_agents_roles ON ai_agents (roles);

-- AutomatedPipelines
CREATE INDEX IF NOT EXISTS idx_automated_pipelines_roles ON automated_pipelines (roles);

-- 17 FK index(es) declared.
