-- ============================================================================
-- CREATE TABLES (check-add mode) - Idempotent schema, never drops columns
-- ============================================================================
-- Generated by rulebook-to-postgres tool
-- Mode: check-add    Stage: dev
-- Total Tables: 2
-- Total Raw Fields: 10
-- ============================================================================

SET timezone = 'UTC';

-- ============================================================================
-- CREATE TABLES - Normalized data only (no calculated fields)
-- ============================================================================

-- ----------------------------------------------------------------------------
-- Charges: Individual charged particles with position and mass
-- ----------------------------------------------------------------------------
CREATE TABLE IF NOT EXISTS charges (
  charge_id                           TEXT                 PRIMARY KEY          -- Unique identifier
);
ALTER TABLE charges ADD COLUMN IF NOT EXISTS label TEXT;                                            -- Human-readable label
ALTER TABLE charges ADD COLUMN IF NOT EXISTS charge_value NUMERIC;                                  -- Charge in Coulombs
ALTER TABLE charges ADD COLUMN IF NOT EXISTS position_x NUMERIC;                                    -- X coordinate
ALTER TABLE charges ADD COLUMN IF NOT EXISTS position_y NUMERIC;                                    -- Y coordinate
ALTER TABLE charges ADD COLUMN IF NOT EXISTS position_z NUMERIC;                                    -- Z coordinate
ALTER TABLE charges ADD COLUMN IF NOT EXISTS mass NUMERIC;                                          -- Mass in kg
COMMENT ON TABLE charges IS 'Individual charged particles with position and mass';
COMMENT ON COLUMN charges.charge_id IS 'Unique identifier';
COMMENT ON COLUMN charges.label IS 'Human-readable label';
COMMENT ON COLUMN charges.charge_value IS 'Charge in Coulombs';
COMMENT ON COLUMN charges.position_x IS 'X coordinate';
COMMENT ON COLUMN charges.position_y IS 'Y coordinate';
COMMENT ON COLUMN charges.position_z IS 'Z coordinate';
COMMENT ON COLUMN charges.mass IS 'Mass in kg';

-- ----------------------------------------------------------------------------
-- ChargeInteractions: Pairs of charges with computed electrostatic force
-- ----------------------------------------------------------------------------
CREATE TABLE IF NOT EXISTS charge_interactions (
  interaction_id                      TEXT                 PRIMARY KEY          -- Interaction ID
);
ALTER TABLE charge_interactions ADD COLUMN IF NOT EXISTS charge1_id TEXT;                           -- First charge
ALTER TABLE charge_interactions ADD COLUMN IF NOT EXISTS charge2_id TEXT;                           -- Second charge
COMMENT ON TABLE charge_interactions IS 'Pairs of charges with computed electrostatic force';
COMMENT ON COLUMN charge_interactions.interaction_id IS 'Interaction ID';
COMMENT ON COLUMN charge_interactions.charge1_id IS 'First charge';
COMMENT ON COLUMN charge_interactions.charge2_id IS 'Second charge';

-- ============================================================================
-- FK INDEXES - Performance only, always applied
-- ============================================================================

-- (no FK fields detected in rulebook)
