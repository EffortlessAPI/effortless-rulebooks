-- ============================================================================
-- CREATE TABLES (check-add mode) - Idempotent schema, never drops columns
-- ============================================================================
-- Generated by rulebook-to-postgres tool
-- Mode: check-add    Stage: dev
-- Total Tables: 5
-- Total Raw Fields: 27
-- ============================================================================

SET timezone = 'UTC';

-- ============================================================================
-- CREATE TABLES - Normalized data only (no calculated fields)
-- ============================================================================

-- ----------------------------------------------------------------------------
-- Users: Login identities for the demo (stub auth).
-- ----------------------------------------------------------------------------
CREATE TABLE IF NOT EXISTS users (
  user_id                             TEXT                 PRIMARY KEY
);
ALTER TABLE users ADD COLUMN IF NOT EXISTS name TEXT;
ALTER TABLE users ADD COLUMN IF NOT EXISTS email TEXT;
ALTER TABLE users ADD COLUMN IF NOT EXISTS role TEXT;                                               -- trainer | client | admin
ALTER TABLE users ADD COLUMN IF NOT EXISTS display_name TEXT;
COMMENT ON TABLE users IS 'Login identities for the demo (stub auth).';
COMMENT ON COLUMN users.role IS 'trainer | client | admin';

-- ----------------------------------------------------------------------------
-- Trainers: Personal trainers who log sessions and bill clients.
-- ----------------------------------------------------------------------------
CREATE TABLE IF NOT EXISTS trainers (
  trainer_id                          TEXT                 PRIMARY KEY
);
ALTER TABLE trainers ADD COLUMN IF NOT EXISTS name TEXT;
ALTER TABLE trainers ADD COLUMN IF NOT EXISTS email TEXT;
ALTER TABLE trainers ADD COLUMN IF NOT EXISTS full_name TEXT;
ALTER TABLE trainers ADD COLUMN IF NOT EXISTS hourly_rate NUMERIC;                                  -- Default $/hour charged to clients.
COMMENT ON TABLE trainers IS 'Personal trainers who log sessions and bill clients.';
COMMENT ON COLUMN trainers.hourly_rate IS 'Default $/hour charged to clients.';

-- ----------------------------------------------------------------------------
-- Clients: Trainer's clients.
-- ----------------------------------------------------------------------------
CREATE TABLE IF NOT EXISTS clients (
  client_id                           TEXT                 PRIMARY KEY
);
ALTER TABLE clients ADD COLUMN IF NOT EXISTS name TEXT;
ALTER TABLE clients ADD COLUMN IF NOT EXISTS email TEXT;
ALTER TABLE clients ADD COLUMN IF NOT EXISTS full_name TEXT;
ALTER TABLE clients ADD COLUMN IF NOT EXISTS trainer TEXT;
COMMENT ON TABLE clients IS 'Trainer''s clients.';

-- ----------------------------------------------------------------------------
-- Invoices: An invoice bundles one or more sessions billed to a client.
-- ----------------------------------------------------------------------------
CREATE TABLE IF NOT EXISTS invoices (
  invoice_id                          TEXT                 PRIMARY KEY
);
ALTER TABLE invoices ADD COLUMN IF NOT EXISTS name TEXT;
ALTER TABLE invoices ADD COLUMN IF NOT EXISTS client TEXT;
ALTER TABLE invoices ADD COLUMN IF NOT EXISTS issue_date TIMESTAMPTZ;
ALTER TABLE invoices ADD COLUMN IF NOT EXISTS due_date TIMESTAMPTZ;
ALTER TABLE invoices ADD COLUMN IF NOT EXISTS tax_rate NUMERIC;                                     -- Decimal (e.g. 0.08 = 8%).
ALTER TABLE invoices ADD COLUMN IF NOT EXISTS discount_amount NUMERIC;
ALTER TABLE invoices ADD COLUMN IF NOT EXISTS paid_amount NUMERIC;
COMMENT ON TABLE invoices IS 'An invoice bundles one or more sessions billed to a client.';
COMMENT ON COLUMN invoices.tax_rate IS 'Decimal (e.g. 0.08 = 8%).';

-- ----------------------------------------------------------------------------
-- Sessions: A logged training session. The atomic line item that rolls up into an invoice.
-- ----------------------------------------------------------------------------
CREATE TABLE IF NOT EXISTS sessions (
  session_id                          TEXT                 PRIMARY KEY
);
ALTER TABLE sessions ADD COLUMN IF NOT EXISTS name TEXT;
ALTER TABLE sessions ADD COLUMN IF NOT EXISTS client TEXT;
ALTER TABLE sessions ADD COLUMN IF NOT EXISTS session_date TIMESTAMPTZ;
ALTER TABLE sessions ADD COLUMN IF NOT EXISTS duration_hours NUMERIC;
ALTER TABLE sessions ADD COLUMN IF NOT EXISTS rate_override NUMERIC;                                -- If > 0, overrides the trainer's HourlyRate for this session.
ALTER TABLE sessions ADD COLUMN IF NOT EXISTS notes TEXT;
ALTER TABLE sessions ADD COLUMN IF NOT EXISTS invoice TEXT;
COMMENT ON TABLE sessions IS 'A logged training session. The atomic line item that rolls up into an invoice.';
COMMENT ON COLUMN sessions.rate_override IS 'If > 0, overrides the trainer''s HourlyRate for this session.';

-- ============================================================================
-- FK INDEXES - Performance only, always applied
-- ============================================================================

-- Clients
CREATE INDEX IF NOT EXISTS idx_clients_trainer ON clients (trainer);

-- Invoices
CREATE INDEX IF NOT EXISTS idx_invoices_client ON invoices (client);

-- Sessions
CREATE INDEX IF NOT EXISTS idx_sessions_client ON sessions (client);
CREATE INDEX IF NOT EXISTS idx_sessions_invoice ON sessions (invoice);

-- 4 FK index(es) declared.
