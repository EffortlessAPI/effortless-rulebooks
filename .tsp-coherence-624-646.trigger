run-id: tsp-coherence-624-646-attempt-6
intent: explicitly re-dispatch the primary temporal runner from durable loop 640; close loops 641-646, build and test Postgres, and retain every prior loop commit unchanged
